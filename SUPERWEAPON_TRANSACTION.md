# 即时超武事务边界

## 已确认的字段

超武对象由玩家根对象 `+0x1A0` 指向的数组取得。当前只接受合法对象地址，并读取：

- `+0x48`：活动状态；
- `+0x56`：一次性/机会标志；
- `+0x57`：就绪标志；
- `+0x58`：等待标志；
- `+0x2C/+0x30/+0x34`：冷却/时间相关字段。

## 应用规则

应用前必须同时满足：

1. Steam 版本 SHA-256 匹配；
2. 游戏 PID、玩家根对象和超武对象数组有效；
3. 已捕获快照与当前 PID、根对象、对象指针完全一致；
4. 所有写入字段仍与快照一致；
5. 应用只写入已验证的超武字段，不执行远程线程，不修改发射流程。

## 关闭规则

关闭不能无条件恢复整个旧快照，因为用户可能已经发射过超武，字段已由游戏自然变化。关闭时必须先确认对象仍处于本模块写入的“就绪状态”：

- `+0x48=0`；
- `+0x57=1`；
- `+0x58=0`；
- `+0x30=0`；
- `+0x34=0`。

如果任一对象已经被发射、进入冷却、重建或替换，关闭操作应拒绝恢复并提示重新载入任务，而不是覆盖游戏的自然状态。

## 当前状态

- 只读状态：已接入统一状态检查器；
- 快照：已完成并绑定版本、PID、根对象和对象指针；
- 应用：尚未接入产品控制器；
- 应用/关闭恢复：严格校验原型已通过。应用前要求对象字段与快照一致；关闭前要求对象仍处于本模块写入的就绪状态；恢复成功后清除快照。实机测试未发射超武，结果为 `SUPER=ON → SUPER=OFF RESTORED`，最终 7 个对象恢复原状态，`snapshot=ABSENT`。
