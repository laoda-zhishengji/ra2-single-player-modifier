# 红警2原版单机修改器：消费级产品阶段计划

## 目标

将已经完成单机验证的功能，从一次性测试工具和后台服务，整理为一个面向 Steam《红色警戒2》原版单机的统一修改器：默认安全关闭、功能独立开关、版本不匹配拒绝运行、关闭可恢复、异常退出不留下磁盘补丁，并向用户明确显示当前状态。

范围固定为：当前 Steam 原版 `game.exe` 单机战役与遭遇战。暂不支持联机、局域网、MOD、尤里的复仇、Ares 或其他版本。

## 暂缓项目

“即时生产下多个单位共用同一出口”暂缓，不进入成品功能列表。现有证据表明即时完成入口会被高频重复触发，但尚未找到稳定且不破坏原版出厂规则的修复点。相关探针、硬件断点和实验代码只保留在研究目录，不得被产品启动流程调用。

## 成品功能清单

| 功能 | 当前基础 | 产品阶段要求 |
|---|---|---|
| 金钱不减 | 单机验证通过 | 接入统一事务开关，允许收入与奖励正常增加 |
| 电力无负载 | 单机与恢复验证通过 | 接入统一事务开关，验证拆除电厂和任务切换 |
| 即时建造与招募 | 多类队列验证通过 | 保留现有逻辑；补齐取消队列、关闭恢复和异常退出测试 |
| 即时超武 | 原版超武和伞兵释放验证通过 | 每个超武独立状态检查，统一开关与恢复 |
| 解锁迷雾 | 开启、关闭、小地图恢复已验证 | 任务载入、存档载入、雷达存在/失效回归 |
| 无视建筑距离 | 地形、水陆、占用、AI 和恢复已验证 | 接入统一事务开关，保留所有非距离合法性检查 |
| 原生自动修理 | 单机验证通过 | 独立开关，明确使用原生修理规则和延迟 |
| 驻军民用建筑自动修理 | 对象筛选和原版维修标志验证通过 | 独立开关，只作用玩家所属、受损、已驻军民用建筑 |

## 产品架构

### 1. 启动器与状态中心

- 只接受匹配路径和匹配 SHA-256 的 `game.exe`。
- 启动时只读检查版本、进程位数、模块基址和目标字节。
- 版本不匹配时所有“应用”按钮禁用，并说明原因。
- 未发现游戏时显示等待状态，不自行启动 Steam，不注入其他进程。
- 记录产品版本、游戏版本、功能状态和失败原因。

### 2. 功能模块

每个功能都实现统一接口：

```text
检查当前状态 → 保存原始状态 → 应用 → 验证已生效 → 显示状态
关闭 → 验证当前仍属于本模块 → 恢复 → 再次验证
```

模块不能互相依赖，也不能用“当前不是预期值就强行覆盖”的方式恢复。发现字节、指针或字段被其他程序改变时，必须拒绝恢复并提示用户。

### 3. 生命周期管理

- 游戏进程退出时停止所有后台服务。
- 功能开关关闭时立即停止对应线程或服务，并恢复可恢复状态。
- 游戏重新载入任务时，按模块规则重新获取运行时对象，不沿用旧指针。
- 任何远程内存写入都必须在写入后刷新指令缓存，并记录地址、旧值、新值和结果。
- 不修改磁盘上的 `game.exe`、`rules.ini` 或存档。

### 4. 日志与诊断

提供用户可导出的日志，至少包括：

- 版本校验结果；
- 游戏 PID 与匹配路径；
- 各功能的开启、关闭、验证、恢复结果；
- 拒绝操作的原因；
- 游戏异常退出时的最后状态。

日志不记录 Steam 登录信息、账号信息或联机数据。

## 消费级界面要求

首版界面只保留必要内容：

- 顶部：游戏状态、版本状态、当前 PID；
- 中部：功能开关和简短说明；
- 底部：应用结果、恢复结果、打开日志；
- 明确标识“仅单机，不支持联机”；
- 默认全部关闭；
- 不把研究探针、地址、偏移和实验按钮暴露给普通用户。

每项功能显示三态：

```text
未启用 / 已启用 / 不可用
```

“不可用”必须附带明确原因，例如版本不匹配、游戏未进入任务、目标对象尚未创建或当前状态不允许操作。

## 分阶段实施

### 阶段 A：统一安全底座

1. 合并路径与 SHA-256 校验，移除各工具重复实现。
2. 建立统一模块描述、状态码和日志格式。
3. 实现事务式应用/恢复框架。
4. 增加进程退出监测和功能线程安全停止。
5. 明确区分“只读检查工具”和“正式产品模块”。

验收门槛：版本错误时零写入；同一功能重复开启不会重复分配或重复打补丁；关闭后状态与开启前一致。

### 阶段 B：合并已完成核心功能

优先接入金钱不减、电力无负载、即时建造与招募。三项均使用现有已验证逻辑，但必须补齐：

- 独立开关；
- 应用后状态验证；
- 关闭恢复；
- 游戏退出后的清理；
- 任务切换和存档载入回归。

### 阶段 C：接入稳定功能

接入即时超武、解锁迷雾和无视建筑距离。重点验证跨任务状态、雷达建筑变化、小地图恢复、建筑地形约束和 AI 边界。

### 阶段 D：接入自动修理

将原生自动修理与驻军民用建筑自动修理作为两个独立模块，明确两者的不同实现和不同关闭逻辑。驻军模块只处理玩家所属、已驻军、受损的民用建筑，不直接写生命值。

### 阶段 E：包装与发布验收

- Win32 发布构建；
- 清晰的安装目录和卸载流程；
- 不捆绑调试器、探针、研究脚本；
- 首次启动自检；
- 版本不匹配保护；
- 全功能矩阵测试；
- 异常退出、重复开启、关闭恢复和重新进入任务测试。

## 首版发布阻断项

以下任一项未通过，不能称为消费级成品：

1. 任一功能可能在版本不匹配时写入游戏；
2. 任一功能关闭后无法可靠恢复；
3. 游戏异常退出后会留下磁盘补丁或后台服务；
4. 多个功能同时开启时会互相覆盖或破坏恢复值；
5. 用户无法看懂当前功能是否生效；
6. 研究工具仍能从普通启动流程触发；
7. 单机功能边界无法在界面和日志中明确说明。

## 当前结论

项目已经从“继续寻找新功能”转入“统一封装与质量化”阶段。下一步实现顺序固定为：安全底座 → 三项核心功能合并 → 超武/迷雾/建筑距离 → 两类自动修理 → 发布验收。

## 当前已落地的底座组件

已新增只读状态检查器 `build-vs\\Release\\ra2_product_status.exe`。它集中完成目标进程识别、Steam 版本 SHA-256 校验、金钱/电力补丁状态读取和生产运行时字段读取，并明确输出 `writes=NONE`。它将作为后续界面和统一模块管理器的状态层基础。

详细验收项已整理至 `PRODUCT_TEST_MATRIX.md`。当前下一项是实现重复开启保护和模块级原值恢复，之后才进入普通用户界面。

已新增 `product/ra2_product_module.h/.c` 模块状态层，并通过 `ra2_product_module_test.exe` 验证：重复开启会被拒绝；恢复时发现当前值与快照不一致会进入错误态而拒绝恢复；值一致时才能完成恢复。

状态检查器已接入该模块层，当前输出 `module_registry=READY count=8`，并继续报告 `writes=NONE`。后续金钱、电力和生产管理器将直接使用这份注册表。

已新增 `build-vs\\Release\\ra2_product_control.exe`，首轮统一接入金钱和电力两个字节补丁，支持只读 `--status` 以及独立的开启/关闭命令。当前已验证 `--status` 输出版本匹配、`money=OFF`、`power=OFF`、`WRITES=NONE`；本轮没有开启任何功能。

随后完成了两个模块的实机控制闭环：金钱模块 `2B C7 → 90 90 → 2B C7`，电力模块 `03 C8 → 90 90 → 03 C8`。两个模块分别开启和关闭，互不影响；最终状态均为 `OFF`，状态检查器报告 `WRITES=NONE`。

即时建造本轮暂不接入统一控制入口。只读基线显示当前生产运行时字段为 `1,2,2,2,2`，且没有活动即时生产服务；旧版高频服务不具备产品所需的原值快照和安全停止保证。它必须先改造成“每个活动队列独立保存原值、一次性应用、可验证恢复”的模块，才能进入成品。

已新增 `build-vs\\Release\\ra2_product_production.exe` 的安全事务原型：`--status` 只读当前字段，`--capture` 保存版本绑定的原值快照，`--apply` 仅在当前字段仍与快照一致时一次性应用，`--restore` 仅在当前字段仍为应用值时恢复。当前只验证了 `--status`：版本匹配、字段为 `1,2,2,2,2`、快照不存在、`WRITES=NONE`；尚未应用即时生产。

已进一步验证 `--capture`：成功生成 160 字节的版本绑定快照，随后 `--status` 报告 `SNAPSHOT=PRESENT`，游戏字段仍为 `1,2,2,2,2`，整个过程 `WRITES=NONE`。下一步才是受控的应用—恢复实机闭环。

应用—恢复闭环已通过：`1,2,2,2,2 → 15,15,15,15,15 → 1,2,2,2,2`。应用过程没有后台循环，恢复前校验通过，最终生产字段回到快照原值。该模块现在具备进入更完整功能回归的基础，但仍需测试任务切换、存档载入、取消生产和异常退出。

生命周期收口也已通过：恢复成功后自动清除快照文件，最终状态为 `SNAPSHOT=ABSENT`、生产字段恢复为 `1,2,2,2,2`。这样旧任务的快照不会长期残留并影响后续任务。

快照保护已加强：快照现在同时绑定游戏 PID 和任务对象根指针。若游戏重启、任务切换或运行时根对象变化，应用/恢复会拒绝写入；同一进程和同一任务状态的应用—恢复回归已重新通过。

已新增 `product/ra2_product_manifest.h`，并将状态检查器、金钱/电力控制器和即时建造模块统一改为引用同一份游戏路径、SHA-256、玩家根指针和关键地址清单。三项程序重新编译并完成只读回归，均报告版本匹配且 `WRITES=NONE`。

即时超武前置盘点已接入统一状态检查器，并增加对象地址合法性过滤，避免把数组中的小整数误判为对象。当前只读识别到 7 个有效超武对象，其中 2 个处于 `ready=1`，全过程 `WRITES=NONE`。超武写入逻辑仍未接入产品控制器。

已新增 `build-vs\\Release\\ra2_product_super.exe`，当前支持 `--status` 和 `--capture`。它保存版本、PID、任务根指针、超武对象指针以及 `+0x48/+0x56/+0x57/+0x58/+0x2C/+0x30/+0x34` 字段；当前已捕获 7 个有效对象，快照大小 308 字节，过程无游戏内存写入。应用/恢复仍待下一步设计和验证。

超武严格事务原型已完成应用—恢复测试：7 个对象从快照状态切换为就绪，再恢复原始状态；恢复后快照自动清除，最终状态为 `snapshot=ABSENT`。测试未发射超武，因此“发射后关闭”的拒绝路径仍需单独回归。

解锁迷雾模块已新增 `build-vs\\Release\\ra2_product_fog.exe`，支持 `--status/--on/--off`，统一使用版本清单，并通过一次性远程调用执行原版开启和迷雾重算流程，不常驻、不修改磁盘文件。本轮构建成功，但运行时游戏已退出，因此 `--status` 返回 `TARGET_UNAVAILABLE`；尚未对游戏执行开启或关闭操作。

无视建筑距离模块已新增正式构建目标 `build-vs\\Release\\ra2_product_build_distance.exe`，并改为引用统一版本清单。现有双调用点事务逻辑、未知字节拒绝和失败回滚保持不变；本轮只完成构建，因游戏未运行未执行探测或应用。

自动修理审计发现并修正了一个实现边界：C 版驻军民用建筑服务原先直接递增生命值，已改为只写原生 `+0x5B8` 维修标志；同时接入统一版本清单并新增正式构建目标 `build-vs\\Release\\ra2_product_garrison_repair.exe`。普通自动修理服务仍保持原生维修标志路径，两个服务本轮均未启动。

普通自动修理也已接入统一版本清单，并新增 `build-vs\\Release\\ra2_product_auto_repair.exe`。两个服务均成功编译；当前不自动启动，后续由统一控制器负责生命周期、独立开关和停止清理。

已新增并编译统一服务控制器 `build-vs\\Release\\ra2_product_services.exe`，支持普通自动修理与驻军民用建筑修理的独立状态、开启、关闭及全部关闭。控制器已改为引用统一游戏路径清单；两个修理服务的停止事件改为可创建/可复用的命名事件，避免服务先于控制器启动时无法协作停止。本轮在游戏未运行状态下验证 `--status` 和 `--all-off` 均安全返回，两个服务均为 `OFF`。

状态检查器已扩展为同时报告 `auto_repair` 和 `garrison_repair` 服务状态；即使游戏未运行，也会先报告两个服务均为 `OFF`，再返回 `game=NOT_FOUND`。本轮重新编译并完成只读回归，输出 `module_registry=READY count=8`、两个修理模块 `OFF`，未执行任何游戏内存写入。

已新增统一命令入口 `build-vs\\Release\\ra2_product_hub.exe`，将状态、金钱、电力、生产、超武、迷雾、建筑距离和两类修理的现有独立工具收拢到同一组产品命令中。入口只做固定路由，不绕过各模块自身的版本校验、快照校验和恢复保护；本轮已验证统一 `--status`、`--repair-status` 和 `--production-status` 的转发结果正确。当前游戏未运行，因此生产状态安全返回 `TARGET_UNAVAILABLE`。

两个自动修理服务已补上进程生命周期退出条件：目标游戏 PID 消失后连续确认约一秒，服务自行退出，不再无限空转等待。服务的对象筛选、原生维修标志和玩家归属逻辑未改变；本轮重新编译通过，待下一次游戏实机回归验证退出清理。

已新增最小消费级界面 `build-vs\\Release\\ra2_product_ui.exe`：显示游戏/版本状态、提供各功能的独立开启与关闭按钮，并把统一入口输出写入可滚动日志框。界面默认不启动游戏、不自动开启功能；底层工具仍负责版本、对象和快照保护。本轮完成 Win32 编译，实机按钮回归待游戏启动后进行。

界面已补齐普通自动修理与驻军建筑修理的独立关闭按钮，并在每次操作后自动刷新总状态；重新编译通过。这样用户不会依赖旧的界面文字判断功能状态，最终状态以统一状态检查器返回值为准。

界面启动流程已调整为先执行 `--self-check`，再读取完整模块状态；启动检查与状态检查均为只读操作。UI 已重新编译通过。

统一入口的 `--status` 已升级为完整汇总：依次查询核心状态、两类修理、生产、超武、迷雾和建筑距离模块；一次刷新即可看到各模块是否可用及目标游戏是否存在。无游戏时各模块安全返回不可用状态，不执行写入。

统一入口新增 `--self-check` 发布目录自检，逐项确认状态、控制、生产、超武、迷雾、建筑距离、修理服务和界面文件存在。本轮自检输出 `SELF_CHECK=PASS`，未启动游戏、未启动修理服务、未写入游戏内存。

已新增便携发布打包脚本 `release\\package.ps1`，生成 `dist\\RA2SinglePlayerModifier`，仅复制 11 个产品组件和用户说明，不复制研究探针、调试器、源码或实验工具。发布目录自检已通过 `SELF_CHECK=PASS`。

打包脚本已收口为“复制后自动自检”：本轮重新执行后输出 `PACKAGE_SELF_CHECK=PASS`，自检失败会以错误退出，不生成可误用的完整性不明发布包。

发布包已加入 `RELEASE_MANIFEST.txt`，记录测试版号、目标游戏 SHA-256、单机范围和当前测试状态；本轮重新打包并通过完整性自检。

发布脚本已增加异常文件拒绝：发布目录若混入研究探针、源码、调试器或其他未列入白名单的文件，打包会失败且不会自动删除；当前发布目录通过白名单检查。

产品 C 组件已使用静态 MSVC 运行库重新构建；依赖检查显示统一入口只依赖系统 `KERNEL32.dll`，状态模块依赖系统 `bcrypt.dll`，不再依赖额外的 VC++ 运行库 DLL。发布包已重新生成并通过自检。

已新增 `RELEASE_ACCEPTANCE.md`，将离线已通过项、下一次实机回归顺序和发布阻断项分开记录；当前仍明确标记为测试版，未把缺少游戏运行时证据的项目提前标记为完成。
## 2026-09-08 最新状态

产品已进入“消费级 UI + 实机功能验收”阶段。当前发布包为 `0.1.1-ui-preview`，已经具备：

- 无边框红警主题界面、游戏图标、功能图标和主视觉素材；
- 功能分组、整行点击切换、操作结果反馈和自动连接监视；
- 游戏未连接或尚未进入任务时自动锁定功能；
- Hub 状态汇总将 `NOT_IN_TASK` 视为正常等待状态，不误报为连接失败；
- 发布目录白名单、素材打包和 `PACKAGE_SELF_CHECK=PASS`。

当前仍不能提前宣称完成的部分是进入单人任务后的生产、超武、迷雾、建筑距离和两类维修实机回归；这些必须在真实任务状态下逐项验证。
