# Steam《红色警戒2》原版单机修改器

## 1. 项目目标

制作一个面向普通用户的消费级桌面修改器，仅支持 Steam 版《红色警戒2》原版的单机模式。

产品应当易用、稳定、可恢复、可诊断，并在版本不匹配时安全拒绝运行。

## 2. 明确不在范围内的内容

- 《尤里的复仇》兼容
- 联机、局域网和任何网络对战功能
- DLL 注入、隐藏注入或规避安全检测
- 修改 Steam 客户端
- 分发游戏本体或受版权保护的游戏资源

## 3. 产品原则

1. 默认不修改游戏文件。
2. 默认不开启任何功能，所有修改都由用户明确启用。
3. 只针对已确认的游戏版本工作。
4. 每个功能都必须具备关闭、恢复和异常保护机制。
5. 补丁失败时不影响游戏正常启动和退出。
6. 任何涉及联机状态的场景都应主动禁用修改器功能。

## 4. 技术路线调整

项目不再采用 DLL 注入、远程线程或进程内 Hook。改为由外部桌面程序打开目标进程，在完成版本校验后对目标进程内的指定代码字节进行临时补丁。

“无限金钱”的目标实现方式是：定位并验证真正的玩家扣款指令，将扣款操作临时替换为等效空操作；关闭功能或退出游戏时恢复原始字节。资金显示值不进行定时锁定和反复写入。

该路线仍然只适用于单机进程，且必须具备原始字节校验、补丁状态检测和自动回滚。

## 5. 分阶段计划

### 阶段 A：环境与版本基线

- 确认 Steam 原版的实际启动方式和目标进程。
- 记录可执行文件的版本、架构、文件校验信息和模块列表。
- 确定测试系统、权限条件和干净存档。
- 建立“版本匹配 / 不匹配 / 未知版本”三种状态。

验收标准：能够准确识别目标版本；不匹配时不执行后续修改。

### 阶段 B：外部补丁验证

- 建立外部进程访问和版本校验模块。
- 只读验证候选代码位置及原始字节。
- 在独立测试副本中验证临时补丁和原始字节恢复。
- 覆盖重复启动、快速退出、切换窗口和异常退出场景。

验收标准：不加载 DLL；不创建远程线程；补丁失败时游戏仍可正常运行；关闭后原始字节恢复。

### 阶段 C：单机功能原型

- 确认玩家扣款指令及其机器码特征。
- 设计最小字节补丁，避免改变周边指令和栈状态。
- 增加明确的启用状态、状态反馈和关闭恢复。
- 记录功能启用失败的原因，不暴露地址、偏移等内部细节。

验收标准：购买时不扣除资金；收入和金币箱仍可增加资金；关闭后正常扣款；存档和重新载入不产生异常。

### 阶段 D：消费级界面与诊断

- 提供简洁的桌面界面。
- 自动检测 Steam 游戏路径和启动状态。
- 提供版本提示、功能开关、错误说明和日志导出。
- 增加安全模式和“一键停止修改”。

验收标准：普通用户无需了解进程、地址或 DLL 即可完成操作。

### 阶段 E：质量与发布

- 建立回归测试清单。
- 验证管理员权限、杀毒软件误报、不同分辨率和常见安装路径。
- 提供安装、升级、卸载和配置备份。
- 发布前确认不会包含游戏文件和联机功能。

验收标准：安装、使用、关闭、卸载完整闭环可重复完成。

## 6. 首版建议功能

首版只实现少量单机功能，优先选择可验证、可撤销、不会破坏存档的功能。每个功能都必须经过独立测试后才能加入正式版本。

## 7. 关键风险

- Steam 或社区补丁导致版本变化。
- 老式 32 位程序与现代 Windows 权限、显示和兼容性设置冲突。
- 修改内部逻辑造成崩溃、不同步或存档异常。
- 安全软件将注入组件误判为风险程序。
- 用户误将修改器用于网络对战。
- 错误字节补丁可能导致立即崩溃，因此必须先做原始字节匹配。
- 游戏异常退出时补丁可能来不及恢复，但补丁只存在于进程内存，不修改磁盘文件。

## 8. 当前下一步

先完成阶段 B：建立只读的外部代码位置验证器，确认候选扣款指令的原始字节和执行条件，然后再设计临时补丁。

## 8. 已记录的本机基线

- Steam 安装目录：`C:\Program Files (x86)\Steam\steamapps\common\Command & Conquer Red Alert II`
- 启动链：`Ra2.exe` 启动 `game.exe`
- 实际游戏窗口进程：`game.exe`
- 主程序显示版本：`1.08`
- `game.exe` SHA-256：`73288C03B58D370BE268CA6D156B4E33BFDB2066DC980359467D8852FF3B00DF`
- 主程序文件描述：`Main executable for Red Alert 2`
- 原始文件名：`Sun.exe`
- 当前研究目标：以 `game.exe` 为 DLL 生命周期目标
- 已确认存在 Steam Overlay 模块；后续测试需记录其共存情况

## 9. 阶段 B 当前状态

- 已建立最小 DLL、目标进程校验加载器和构建说明。
- 当前尚未执行注入，也未修改游戏内存或游戏文件。
- 本机 MinGW 编译环境在链接阶段缺少 `collect2.exe`，暂时无法生成可执行测试文件。
- 下一步：使用完整的 Win32 编译工具链构建，再进行加载 / 卸载日志验收。

## 10. 阶段 B 验收结果

- 已采用 Visual Studio 2022 Build Tools，MSVC `19.44.35228.0`。
- 已使用 Windows SDK `10.0.26100.0`。
- `ra2_probe.dll` 已成功编译为 `14C machine (x86)`。
- `ra2_probe_loader.exe` 已成功编译为 `14C machine (x86)`。
- 加载器已识别正确的 Steam `game.exe`，并成功加载 DLL。
- 已确认目标进程中存在 `ra2_probe.dll`。
- 已写入 `%TEMP%\\ra2_modifier_probe.log` 的 `attach` 事件。
- 当前游戏仍在运行，`detach` 事件将在正常退出后验证。
- WinLibs 已通过包管理器卸载，安装目录已移除，PATH 未残留。
- 已正常退出游戏，`game.exe` 和 `Ra2.exe` 均已结束。
- 已确认日志记录 `detach` 事件，阶段 B 生命周期闭环验收通过。

## 11. 阶段 C：无限金钱定位状态

- 已建立只读资金扫描器，目标限定为基线中的 `game.exe`。
- 首次采样值：`12549`。
- 首次扫描结果：`53` 个 32 位整数候选位置。
- 当前结论：单次扫描不能确认真实资金变量，需要通过消费后的第二个数值进行交叉筛选。
- 在确认变量前，不执行任何内存写入。
- 第二次采样值：`11749`，由消费 `800` 得到。
- 第二次扫描结果：16 个匹配，和第一次候选交集为 3 个：`0x008373CC`、`0x008373D0`、`0x236CAFCC`。
- 当前仍处于只读定位阶段。

## 12. 无限金钱原型状态

- 已确认重启后主模块基址仍为 `0x00400000`。
- 已确认 `game.exe+0x4373CC` 与 `game.exe+0x4373D0` 在新进程中仍出现为资金值。
- 已建立并编译 `ra2_money.dll`，目标值为 `999999`。
- 已成功加载原型 DLL，日志记录 `money_feature_started`。
- 只读扫描已发现 `999999` 匹配项，说明原型正在写入目标区域。
- 待用户确认：界面显示是否变为 `999999`，以及消费后是否保持不变。
- 当前原型尚未具备最终产品所需的 UI 开关、版本哈希校验和安全卸载流程。

## 13. 金钱不减原型

- 已建立 `ra2_money_no_decrease.dll`。
- 初始稳定值识别为 `5500`。
- 原型接受不低于当前稳定值的变化，仅在检测到支出后恢复稳定值。
- 当前测试重点：购买行为是否保持资金不减，以及显示是否稳定。

## 14. 函数级扣款拦截方案

- 轮询恢复方案确认会造成显示竞争，不作为正式实现。
- 正式方向改为捕获真实资金写入指令，并在扣款路径上拦截支出。
- 需要在单机购买动作期间使用数据写入断点记录写入指令地址、调用路径和扣款上下文。
- 当前环境没有已安装的 x32dbg、WinDbg 等用户态调试器；静态 `dumpbin` 未直接找到候选地址的绝对引用。
- 在获得写入指令前，不继续猜测 Hook 点，也不把现有轮询 DLL升级为正式功能。

## 15. 自动扣款跟踪

- 已建立自动调试跟踪器，后台附加指定的 x86 `game.exe` 并自动继续调试事件。
- 已确认界面刷新写入集中在 `game.exe+0x94672` 与 `game.exe+0x94681`。
- 反汇编发现候选扣款指令 `sub [edi], eax`，位于 `game.exe+0x39467E`。
- 跟踪器已加入该执行点，将记录 `EDI` 目标地址和 `EAX` 扣款值。
- 当前正在等待一次新的单机购买动作进行确认。

## 16. 扣款路径静态筛选

- 在主程序反汇编中筛出 5 条可能的整数减法写入：`+0x482A45`、`+0x4E5E04`、`+0x5F2B0D`、`+0x79467E`，以及一条目标寄存器不同的候选。
- 自动跟踪器当前对其中 4 条执行点进行无写入跟踪。
- 当前等待一次新的单机购买动作，以确认实际调用路径。

## 17. 测试事故记录

- 在第三次自动跟踪 / 购买测试后，游戏发生错误并退出。
- 当前未发现 `game.exe`、`Ra2.exe` 或跟踪器仍在运行。
- 暂停所有调试器附加、硬件断点和内存写入实验。
- 下一步先改进跟踪器的异常处理与退出清理，再进行 Steam 文件完整性验证和无调试器基线复测。

## 20. 旧资料地址验证

- 当前 Steam 原版 `game.exe+0xA35DB4` 可读并指向有效地址 `0x1F743AA0`。
- 该对象在 `+0x2D0` 至 `+0x300` 范围内均为 `0`，包括资料中的 `+0x2E8`。
- 结论：旧资料中的 `+0x2E8` 偏移不能直接用于当前 Steam 版本。
- 已建立只读指针探测器，未执行任何写入。

## 21. 当前 Steam 版资金复扫

- 当前任务资金基线：`4000`。
- 消费后资金：`3400`。
- 对 `4000` 候选进行定点复扫后，仅剩 `0x008373CC` 和 `0x008373D0` 两个同步变化地址。
- 这两个地址已在重启后的进程中再次出现，但仍需第三组数值确认。
- 当前未启用任何资金写入或调试附加。

## 26. 指针链扫描结果

- 已对当前 Steam 原版进程的可读内存进行指针扫描。
- 未发现直接保存 `0x008373CC` 或 `0x008373D0` 的指针位置。
- 当前推断：资金字段可能通过调用参数、寄存器或对象表间接传递。
- 下一步转向分析字段所在结构和其调用者，不再继续盲目增加指针层级。

## 22. 代码级 Hook 事故

- `ra2_money_hook.dll` 成功完成签名校验并安装，但购买动作后导致游戏闪退。
- 日志显示 DLL 在退出阶段执行了 Hook 移除；游戏文件本身未被永久修改。
- 该 Hook 原型判定为不安全，不再继续加载或迭代到用户游戏进程。
- 后续必须先在离线副本和自建测试函数中验证跳板、寄存器、栈和多线程清理，再考虑重新进入游戏测试。

## 18. 跟踪器修复状态

- 已移除会反复触发单步异常的断点重置逻辑。
- 已加入 120 秒自动超时，避免调试器无限期附着。
- 已加入退出前遍历目标进程线程并清除硬件断点。
- 已加入主动 `DebugActiveProcessStop` 脱离流程。
- 已重新编译成功。
- 已完成无目标进程基线测试：正确返回“未找到目标”状态，未启动调试会话。

## 19. 跟踪器再次失败

- 在干净 `game.exe` 上自动附加后，游戏立即弹窗退出。
- 日志显示 `game.exe+0xE5E04` 是高频执行点，当前硬件执行断点仍会造成大量调试事件。
- 已停止跟踪器，未继续进行内存写入或修改器注入。

## 23. 当前有效路线

前面关于 DLL 注入、调试附加和代码级进程内 Hook 的章节均视为历史实验记录，不再作为实现方案。当前有效方案只有：外部程序 + 版本校验 + 临时进程内存代码补丁 + 原始字节恢复。

当前优先级：

1. 只读确认真正的玩家扣款代码位置。
2. 在独立测试副本中验证最小补丁。
3. 加入补丁前后字节校验、状态检测和自动恢复。
4. 最后再制作用户界面和发布版本。

功能规格已单独整理至 `FEATURES.md`，首版发布门槛为完成金钱不减、电力无负载、即时建造与招募三项，并具备统一的版本校验和回滚机制。

## 24. 金钱不减：外部补丁阶段

- 当前 Steam 原版进程已确认：`game.exe` PID `23608`。
- 文件 SHA-256 仍为已记录的 `73288C03B58D370BE268CA6D156B4E33BFDB2066DC980359467D8852FF3B00DF`。
- 已建立只读补丁探测器，读取 `game.exe+0x94670` 的原始字节。
- 当前读取结果：`89 06 85 C0 0F 9E C1 49 BB 01 00 00 00 23 C1`。
- 尚未执行任何外部代码补丁；下一步先确认该位置是否属于资金更新路径，再设计可恢复的最小补丁。

## 25. 当前版本对象链验证

- 当前资金复扫已再次确认 `0x008373CC` 和 `0x008373D0` 从 `4000` 变为 `3400`。
- 旧资料中的 `game.exe+0xA35DB4` 在当前进程指向 `0x2392D5D0`，其 `+0x24` 为 `0x007A2D74`。
- 该对象的虚表候选 `+0x18` 读取结果为无效填充值 `0xCCCCFFFF`，不能作为当前 Steam 版资金访问路径。
- 当前结论：旧资料地址不适配；`0x008373CC/0x008373D0` 仍属于待进一步分类的资金相关状态，不直接补丁。
- 结论：自制调试器暂不适合继续附加原版游戏；后续改用离线静态分析与独立测试副本，避免再影响正常游戏。

## 27. 金钱字段第三组变化确认

- 在未执行写入、注入或调试附加的情况下，已知资金候选从 `1400` 自然变化到 `2900`。
- 复扫结果：`0x008373CC` 与 `0x008373D0` 均为 `2900`，另有一个堆地址 `0x2392D81C` 为 `2900`。
- 这进一步证明两个固定地址属于资金相关状态，并且自然收入会更新它们；但它们仍不是已证明的扣款指令位置。
- 已加入 `ra2_patch_manager` 外部管理器原型：匹配完整游戏路径、校验 SHA-256、只读检查代码字节，并在版本不匹配时拒绝继续。
- 管理器当前固定输出 `PATCH_STATE=UNAPPLIED`，不执行任何游戏内存写入；待扣款源确认后再接入补丁描述和可恢复字节。
- 已加入独立扣款策略测试：玩家扣款被阻止、玩家收入正常增加、AI 扣款不受影响、功能关闭时保持原逻辑。

## 28. 一次受控购买样本

- 购买前只读基线：资金 `7200`。
- 完成一次普通购买后立即读取：资金 `7000`。
- `0x008373CC` 与 `0x008373D0` 同步从 `7200` 变为 `7000`，确认本次扣款金额为 `200`，且两个字段仍是资金状态镜像或关联字段。
- 购买前后 `game.exe+0x94670` 的 16 字节代码保持不变：`89 06 85 C0 0F 9E C1 49 BB 01 00 00 00 23 C1 89`。
- 本次没有写入内存、注入 DLL 或附加调试器；下一步继续静态追踪扣款调用链。

## 29. 第二次受控购买交叉样本

## 30. 建筑距离差分分析方向

- 旧资料中的 `0049BC1C`、`0049BC2A` 虽然在当前 Steam 版呈现相同字节，但直接 NOP 两个分支不能实现远距离放置，说明它们不是可独立移除的“距离开关”。
- 当前调用链已确认：`0049BBC0` 附近先分别取得两个放置合法性结果；一个来自 `004991D0` 的占用/对象检查，另一个来自 `00499390` 的地形/地图检查。两者都不应整体跳过。
- 下一步采用哨界机枪与碉堡作为差分样本：它们的原版建造范围不同，能在不破坏地形与占用规则的前提下，区分“建筑属性范围”和“统一基地距离检查”。
- 已停止会暂停游戏主线程的断点跟踪器；后续只使用静态调用链、只读内存快照和经过字节校验的最小补丁测试。

## 31. 公开资料修正路线

- 新检索结果显示，RA2 的建造距离不是单一全局常量，而是建筑类型的 `Adjacent` 参数；公开讨论明确提到普通建筑约为 `2`，防御建筑通常更大，且可在 `Rules.ini` 中按建筑类型配置。
- Steam 版仍支持通过外置 `Rules.ini` 覆盖原版规则，但本项目仍坚持不修改游戏文件，因此仅把该机制作为字段语义和结果的验证依据。
- 实现方向改为定位运行时 `BuildingTypeClass` 的 `Adjacent` 字段，并对玩家可建造类型做临时、可恢复的数据级修改；不再尝试 NOP 综合放置分支。

- 第二次购买前资金：`7000`。
- 第二次购买后资金：`6800`，扣款仍为 `200`。
- 对 `scan-current-7000.log` 复扫后仅剩：`0x008373CC`、`0x008373D0`、`0x2392D81C`，三者均为 `6800`。
- 两次购买均未改变 `game.exe+0x94670` 的代码字节；该位置继续排除为直接扣款补丁点。
- 当前结论：资金状态候选已经稳定，但扣款源仍需通过静态调用关系确认，不能直接修改这三个地址。

## 30. 静态候选排除

- 对 `0x0079467E` 的静态反汇编显示，该位置位于十进制数字字符串格式化函数中，执行的是字符缓冲区计数更新，不是玩家资金扣款。
- `0x004E5E04` 虽然包含 `sub [ecx], eax`，但所在函数同样伴随字符串/缓冲区处理，且此前动态观察到的数值很小、频率很高，因此排除为资金扣款点。
- 当前继续沿玩家资源对象的调用关系寻找扣款函数，不再根据单条 `sub` 指令盲目打补丁。

## 31. 资金显示字段职责确认

- 静态调用关系显示：`0x00494510` 初始化 `object+0x4EEC` 字段，`0x00494530` 处理该字段的界面状态，`0x00494650` 重新计算并写入显示值。
- `0x00494650` 的调用者位于 `0x006A124D`，属于资源界面刷新流程；它调用全局对象的虚函数取得当前值，再写入显示结构。
- 结论：`0x008373CC/0x008373D0` 是资金显示或同步状态，不是理想的扣款拦截点；继续向上追踪产生资源变化的业务函数。

## 32. 公开资料机器码交叉验证

- 公开资料给出的原版 RA2 特征字节 `2B C7 89 7C 24 24 89 83 4C 02 00 00 EB 4C` 在当前 Steam `game.exe` 中完整存在。
- 其文件偏移为 `0xE53A9`，按当前 PE 段映射对应运行地址 `0x004E53A9`，即模块偏移 `game.exe+0xE53A9`。
- 当前反汇编为：`sub eax,edi`；随后把结果保存到 `[ebx+0x24C]`。资料所述将首字节 `2B` 改为 `03`，对应把减法改为加法。
- 该发现显著提高了候选可信度，但在写入前仍需确认 `[ebx+0x24C]` 的语义确实是玩家资金，并验证 AI/收入/建筑队列不会被同一共享路径误伤。
- 暂不直接修改；下一步先建立原始字节校验、单字节临时补丁和自动恢复测试。

## 33. 外部临时补丁管理器完成

- `ra2_patch_manager.exe` 已支持只读状态、`--apply` 和 `--restore` 三种模式。
- 默认模式已在当前运行进程通过验证：版本匹配、目标字节为原始 `2B`、完整特征码匹配，未写入游戏。
- 应用模式只写入 1 个字节，并在写入前校验完整版本和原始字节；恢复模式只接受当前字节为 `03` 的状态。
- 当前尚未执行应用模式，等待明确的单机测试授权。

## 34. 加款测试与目标修正

- 首次应用 `2B→03` 后完成一次购买，资金没有减少，反而增加了恰好等于购买价格的金额。
- 这证明该位置确实是扣款路径，但 `03` 是加法，不能满足“金钱不减”的产品需求。
- 已恢复游戏原始字节。
- 管理器目标已修正为 `2B C7→90 90`：跳过 `sub eax,edi`，保留当前资金值；收入路径不被定时器覆盖。
- 新方案尚未应用，需重新编译并进行一次单机验证。

## 35. 金钱不减首轮成功

- 已应用 `game.exe+0xE53A9` 的 `90 90` 临时补丁。
- 用户完成一次购买并确认成功，游戏未闪退。
- 购买后只读读取到两个资金字段均为 `7200`，补丁状态保持 `APPLIED`。
- 当前首要功能“金钱不减”已达到可用验证阶段；仍需补测自然收入和关闭恢复，再进入消费级封装。

## 36. 自然收入与恢复验证

- 用户确认启用补丁期间自然收入正常增加。
- 已自动执行恢复流程，版本校验通过，`90 90` 已恢复为原始 `2B C7`。
- 恢复后的状态为 `PATCH_STATE=UNAPPLIED`，当前游戏代码字节与原始版本一致。

## 37. 电力系统静态定位

- 用户提供当前电力负载基线：`70`。
- 只读扫描 `70` 得到大量普通内存匹配，不能直接作为电力地址，已避免锁定数值路线。
- 当前 `game.exe` 包含可识别的内部符号/字符串：`PowerClass`、`Power.CPP`、`TXT_POWER_DRAIN`、`PowerSurplus`、`IsPowered`、`LowPowerPenaltyModifier`、`PrerequisitePower`。
- `IsPowered` 相关调用点和配置读取路径已找到；下一步需区分建筑对象的供电状态判断与全局负载汇总函数，再确定可恢复补丁点。

## 38. 电力负载汇总点确认

- 公开资料给出的负载字段为 `[0x00A35DB4] + 0x52D4`；当前进程实际读取到该字段，当前值为 `80`。
- 当前版本汇总函数位于 `0x004F2D0F` 附近：每次先将 `[esi+0x52D4]` 清零，然后在 `0x004F2D99` 执行 `add ecx,eax` 累加建筑负载，再于 `0x004F2D9B` 写回。
- 目标补丁候选为 `game.exe+0xF2D99` 的 `03 C8 → 90 90`；因为每轮已经清零，跳过累加即可使最终负载保持为 `0`。
- 该候选比直接锁定 `[0x00A35DB4]+0x52D4` 更稳定，也不会使用定时器反复写值。
- 尚未应用电力补丁；下一步加入统一管理器的校验、应用和恢复流程。

## 39. 电力无负载首轮成功

- `ra2_power_manager.exe` 已接入并通过版本与原始字节校验。
- 已将 `game.exe+0xF2D99` 的 `03 C8` 临时改为 `90 90`。
- 建造并完成一个需要电力的建筑后，负载从 `80` 重算为 `0`。
- 当前指针链 `[0x00A35DB4]+0x52D4` 读取为 `0`，补丁状态为 `APPLIED`。
- 该结果确认“跳过负载累加”路线有效；下一步补测低电量状态、关闭恢复和异常退出恢复。

## 40. 电力补丁关闭恢复验证

- 已执行恢复：`game.exe+0xF2D99` 从 `90 90` 恢复为 `03 C8`。
- 恢复后再次建造并触发电力重算，负载从 `0` 恢复为 `80`。
- 当前状态为 `PATCH_STATE=UNAPPLIED`，证明电力补丁的关闭恢复有效。

## 41. 即时/快速建造的旧版资料迁移验证

- 旧版资料显示，生产速度主要通过 `BuildTimeBuildingsMult`、`BuildTimeDefensesMult`、`BuildTimeInfantryMult`、`BuildTimeUnitsMult` 和 `BuildTimeAircraftMult` 等倍率参与计算；把倍率降到很小可以实现快速生产，但不等于直接跳过生产队列。该思路在旧版 RA2/尤里的复仇资料中均有记录。
- 当前 Steam 原版 `game.exe` 仍包含上述 `BuildTime...` 字符串，以及 `BuildSpeed`、`BuildupTime` 字符串，静态代码也存在读取这些配置并写入生产相关对象字段的路径，说明旧版资料的“机制假设”仍适用。
- 旧版尤里的复仇常见的固定地址和 `gamemd.exe` 偏移不适用于本项目的 `game.exe`，不能直接写入；当前版本必须继续按本文件哈希和当前字节建立独立候选。
- 当前优先候选为生产计时倍率/增量路径，而不是定时锁定界面进度。先做只读字段与代码路径验证，再决定是实现可配置快速生产，还是单独实现即时完成。
- 旧版资料还提示极低倍率可能与多生产建筑、队列初始化产生交互，因此首轮测试必须覆盖建筑、步兵、车辆、飞机和取消队列。

## 42. 当前 Steam 原版生产路径只读确认

- 新增 `ra2_build_probe.exe`，仅执行进程路径、文件哈希和代码字节读取，不写入游戏内存。
- 当前版本哈希校验通过；已确认以下代码路径仍从生产配置读取倍率或时间参数：步兵、车辆、飞机、防御建筑、建筑起造时间和全局建造速度。
- 关键指令均保持预期形式，例如 `push 0x007DACD8`、`push 0x007DACC4`、`push 0x007DACAC`、`push 0x007DAC7C`，以及对 `0x007F0914`、`0x007F0440` 的读取。
- 结论：旧版资料验证通过，机制在当前 Steam 原版仍存在；下一步必须通过一次正在进行的生产队列，确认实际计时字段或完成判定，暂不盲目修改这些配置字符串引用点。

## 43. 生产中的只读动态快照

- 新增 `ra2_build_watch.exe`，按可读页面采集候选整数并在 3 秒后复读，仅输出变化，不写入目标进程。
- 已连续完成两轮快照。`0x0083921C/0x00839220` 等主数据区字段出现小幅变化，但目前更像全局时钟或界面状态，不能直接认定为生产计时器。
- 当前未发现足够稳定、可单独归因于生产队列的字段，因此暂不打补丁；需要继续利用“生产开始—进度变化—完成”三个状态做交叉筛选。

## 44. 旧版 `+0x5378` 候选排除

- 按旧版资料检查当前 Steam 原版全局对象 `0x00A35DB4` 解引用后的 `+0x5360..+0x53A0` 区域。
- 当前 `+0x5378` 及相邻字段均为 `0`，没有出现旧资料中可直接作为生产速度数组的连续有效值；因此旧版固定字段不能迁移到当前版本。
- 当前应继续沿 `BuildSpeed`/`BuildupTime` 的调用路径定位运行时对象字段，避免把零值区域误当作速度控制点。

## 45. 网络资料补充验证

- 多个旧版原版 RA2 1.006 修改器明确提供 `Instant Build`，说明该功能在原版引擎中确实可实现，但资料只记录功能和版本，未公开可迁移的稳定地址。
- 旧版论坛资料把快速生产归因于 `BuildTimeBuildingsMult` 等倍率；另有资料指出原版和尤里复仇的配置项、可执行文件和地址并不相同。
- 公开的 1.006 训练器还常把即时建造、无限电力、地图全开放在同一工具中，但曾出现即时建造同时影响 AI 的反馈，说明消费级版本必须加入“只作用玩家”的边界验证。
- 结论：网上资料进一步确认功能可行，但没有找到可直接套用到当前 Steam 哈希版本的即时建造字节补丁；继续采用当前版本静态路径和运行时对象关联是最快且风险最低的路线。

## 46. 博客资料与当前版本对象链对上

- 用户提供的博客资料给出了原版 RA2 的生产速度字段：`+0x52B8` 飞机、`+0x52BC` 步兵、`+0x52C0` 战车、`+0x52C4` 建筑、`+0x52C8` 船坞，并建议将对应值提高到约 `15`。
- 该资料同时给出的对象根与本项目电力字段使用同一条链：`[game.exe+0x635DB4]` 解引用后访问偏移；来源页面署名为 `viewll`，原文链接由搜索结果定位到对应博客文章。
- 当前 Steam 原版实时读取结果：`+0x52B8=0`、`+0x52BC=3`、`+0x52C0=0`、`+0x52C4=2`、`+0x52C8=0`，`+0x52D4=130`。这与资料描述的“相邻生产速度字段 + 电力负载字段”结构高度吻合。
- 结论：当前版本的快速建造候选已从“推测”升级为“高可信运行时字段候选”。下一步可制作只改这五个生产字段、自动保存原值并支持恢复的外部管理器；暂不触碰 `+0x52D0/+0x52D4` 电力字段。

## 47. 即时建造 CD 路径首轮验证

- 根据旧版资料验证了当前版本的建筑队列指针：`[玩家对象+0x52E8]`，队列对象的 CD 位于 `+0x24`。
- 当前测试中读取到建筑队列对象 `0x214DB760` 的 CD 为 `16`，一次性写入 `53` 后，用户重新建造同类建筑并确认生产就绪。
- 后续读取到新的建筑队列对象 `0x214D9840`，其 CD 为 `54`，说明生产完成状态被正常置为就绪，队列对象和待放置流程没有被破坏。
- 结论：当前 Steam 原版的建筑即时完成路径已首轮验证成功；下一步扩展到步兵、车辆、飞机、船只，并将一次性测试工具封装为自动处理所有活动队列的安全管理器。

## 48. 步兵即时完成验证

- 暂停中的步兵队列读取到对象 `0x214D8190`，CD 为 `23`。
- `ra2_instant_all.exe` 仅将该活动队列的 `+0x24` 从 `23` 推进到 `53`，随后用户确认步兵立即完成并正常出厂。
- 该测试证明建筑和步兵共用同一套“活动队列对象 +0x24 CD”即时完成逻辑；下一步仅需补测车辆、飞机、船只并加入正式功能开关。

## 49. 生产逻辑层即时完成验证

- 当前版本生产函数位于 `0x004B9362` 附近：先读取队列对象 `+0x24`，再增加进度并写回；完成判定阈值为 `0x36`（54）。
- 首次将进度设为 `53` 时，队列停在完成前一格，证明写入点正确但阈值未满足。
- 恢复原始指令后，将同一位置改为直接装载 `54`，用户确认新建筑/单位能够直接完成；该逻辑不依赖外部轮询。
- 当前测试补丁状态为 `APPLIED`，下一步需验证 AI 是否受影响、取消生产是否正常，以及关闭恢复后原版生产是否恢复。

## 50. 超武对象结构初步确认

- 当前玩家对象的超武数组位于 `玩家对象+0x1A0`，可读到多个非空超武对象；这与公开资料中按超武编号索引对象的结构一致。
- 当前对象中可观察到多个相邻编号使用相同的标志字段布局，说明两个伞兵技能即使编号不同，也很可能共用同一类超武对象，而不是两套完全独立的底层逻辑。
- 伞兵是否可立即使用仍需区分：建造/充能 CD、就绪标志、一次性机会标志和归属对象。下一步先在两个伞兵技能分别处于冷却时读取变化，再制作只改充能状态的测试逻辑。

## 51. 网络资料对超武逻辑的补充

- 公开资料指出，超武对象按编号位于 `玩家对象+0x1A0` 数组中；对象的 `+0x57` 是就绪标志，写为 `1` 可使界面进入可用状态，但不一定同步倒计时显示。
- 当前 Steam 原版静态反汇编确认 `0x0069D690` 函数存在，并包含 `mov byte ptr [esi+57h],1`；同一函数还初始化对象的时间字段，说明它比单独写标志更接近完整的“重置超武 CD”逻辑。
- 当前版本还确认存在 `0x0069CCF0` 状态更新函数，以及 `0x0069D610` 的就绪/剩余时间计算路径；其中就绪状态会返回 `0x36`（54）。
- 结论：即时超武应优先模拟或调用完整 CD 重置逻辑，而不是只锁定 `+0x57`。两个伞兵可以复用同一套逻辑，但必须按超武编号和一次性标志分别处理。
- 实测确认：Steam 原版当前对局中，雷电对应内部编号 `2`；仅调用 `0x0069D690` 仍可能要求重新加载，需先将该超武对象的 `+0x48` 清为 `0`，再调用重置函数，才能在当前对局中立即释放。
- 已确认超武映射：`id=0` 为 `MultiSpecial`（核弹），`id=1` 为铁幕，`id=2` 为雷电，`id=3` 为超时空，`id=5/6` 为两种伞兵；核弹还必须保持 `+0x56=1`，否则重置后仍不能直接释放。
- 地图迷雾开关实测成功：调用 `0055A120` 可即时开启全图可见；调用 `0055A070` 可即时触发原版迷雾重算并恢复迷雾。两者均在当前对局生效，未修改游戏文件。

## 52. 功能范围调整

- 根据用户决定，取消“快速建造与招募”功能，不再继续研究、测试或纳入发布版本。
- 保留“即时建造与招募”功能；后续功能顺序调整为即时超武、解锁迷雾、无视建筑距离、自动修理建筑。

## 53. 用户提供的“任意建造”资料与当前 Steam 版对照

- 用户提供的《红色警戒2修改器原理百科（九）》明确区分了两种做法：旧式方案是在 `0049BC1C`、`0049BC2A` 跳过结果分支；更完整的方案是在玩家放置流程的 `0049BBC9` 调用 `004991D0` 后，让该调用返回成功。
- 资料中给出的 `004991D0` 返回约定为 `EAX=1`、`retn 10h`。当前 Steam 版静态反汇编与该约定完全一致：`004991D0` 的正常返回路径位于 `0049937C`，最终也是 `mov al,1`、`ret 10h`。
- 当前 `004991D0` 至少存在两个直接调用点：`004997A0` 和 `0049BBC9`。因此不能把整个 `004991D0` 改成恒真，否则会扩大到其他调用者并可能影响 AI 或非玩家逻辑。
- `0049BB24` 入口使用 `ECX` 作为放置控制对象，并从对象的 `+0x11A0/+0x11A4` 取得当前建造对象和地图/坐标相关数据；`0049BBC9` 位于该玩家放置流程内部。基于此，当前首选方案是只替换 `0049BBC9` 这一处调用目标。
- 新增只针对当前版本哈希的 `patcher/ra2_build_anywhere_test.c`，生成 `build-vs\\Release\\ra2_build_anywhere_test.exe`。它通过远程代码洞让该调用返回 `1` 并执行 `ret 10h`，保留后续地形/地图合法性检查；支持 `--probe`、`--apply`、`--restore`，并校验可执行文件 SHA-256 和原始调用字节。
- 当前只读探测已确认基线为 `0049BBC9: E8 02 D6 FF FF`，状态为 `UNAPPLIED`；尚未应用写入，下一步可在单人测试图中验证哨界机枪、碉堡、普通建筑、陆地/水域、障碍物和 AI 行为。

## 54. 玩家调用点首轮测试无效，转入资料原法诊断

- 用户实测后确认仅替换 `0049BBC9` 的调用目标并未改变建造距离；该补丁已恢复，避免留下无效状态。
- 这说明当前 Steam 版实际距离限制可能由 `004991D0` 的共享路径、另一个放置调用点，或调用返回后的第二阶段逻辑决定；不能继续假设 `0049BBC9` 就是最终入口。
- 新增同一测试器的 `--apply-global/--restore-global` 诊断模式，按资料直接把 `004991D0` 改为 `mov eax,1; ret 10h`。该模式明确标记为全局实验，可能影响其他调用者，不进入正式版本。
- 当前已应用全局诊断补丁，等待一次最小化单人放置测试：只测试哨界机枪或碉堡的远距离位置。测试后必须立即执行 `--restore-global`，再根据结果决定继续追踪第二阶段判定还是改用更窄的入口。

## 55. 全局诊断成功，转向第二调用点

- 用户确认全局 `004991D0` 诊断补丁能够实现远距离建造，证明资料中的核心函数判断正确。
- 全局补丁已立即恢复，避免影响 AI 和其他调用者。
- 当前首个窄调用点 `0049BBC9` 已确认无效；该位置不是当前测试建造操作的实际有效入口，或其结果会被后续路径覆盖。
- 已将第二个直接调用点 `004997A0` 做成独立诊断补丁 `--apply-call2/--restore-call2`，只让该调用返回成功，不改共享函数。
- 当前已应用第二调用点诊断补丁，等待用户重复测试一次哨界机枪或碉堡的远距离放置。若成功，该位置将成为正式实现候选；若仍无效，则继续定位真正的玩家放置调用链。

## 56. 无视建筑距离双调用点方案达到预期

- 用户确认 `0049BBC9` 局部补丁可以让超出原距离的建筑实际落地，但预览仍显示红色。
- 用户进一步确认同时启用 `0049BBC9` 与 `004997A0` 两个局部补丁后，远距离位置显示绿色且点击能够实际放置，达到预期交互效果。
- 全局 `004991D0` 诊断补丁已关闭；当前方案只修改两个已验证的放置调用点，不改共享判定函数，也不修改建筑类型数据。
- 目前结论：无视建筑距离的功能逻辑已验证成功；下一步将把两个局部补丁合并为一个事务式开关，并验证关闭恢复、地形/水陆/占用约束及 AI 行为。

## 57. 无视建筑距离正式开关封装

- 用户已验证地形、水路、占用和 AI 行为均无异常。
- `ra2_build_anywhere_test.exe` 的 `--apply`/`--restore` 已升级为事务式双调用点开关：同时管理 `0049BBC9` 和 `004997A0`，第二处应用失败会自动恢复第一处。
- 恢复按逆序执行，两个代码洞均由补丁自身识别并释放；不依赖重启游戏清理状态。
- 当前游戏进程中的双调用点仍保持已验证的开启状态；可继续进行关闭恢复测试。

## 58. 无视建筑距离关闭恢复完成

- 用户确认关闭功能后，同一远距离位置重新无法放置，原版距离限制已恢复。
- 两个调用点和对应代码洞均已恢复/释放；当前无视建筑距离功能处于关闭状态。
- 功能验收结论：开启时预览绿色且可实际放置，关闭时恢复原版限制；地形、水陆、占用和 AI 边界测试均通过。

## 59. 自动修理只读定位启动

- 根据公开资料，自动修理可通过建筑/单位对象的 `+0x5B8` 修理标志实现；资料还给出了对象虚函数修理调用的思路，但没有可直接迁移到当前 Steam 哈希版本的稳定对象枚举地址。
- 当前 Steam 静态反汇编确认 `+0x5B8` 存在初始化、切换、生命值判断和修理流程引用；其中 `00443699..004436D8` 明确读取并设置该标志，`0044BA46`、`0044BB93` 等路径会依据标志进入修理相关处理。
- 新增只读 `scanner/ra2_building_inventory.c`，按当前版本对象/类型虚表、类型指针和 `+0x5B8` 读取建筑对象、归属指针及生命值，不写入目标进程。
- 首次运行时当前进程没有发现活动建筑实例，未作任何修改；下一步需在任务中拥有至少一座建筑后重新采样，并用受损建筑确认对象枚举和玩家归属字段。

## 60. 自动修理一次性逻辑验证

- 在有己方受损建筑的任务中，已确认当前玩家指针为 `0x1E5C2FE8`，并识别到 `CAWASH06` 与 `GAPILE` 两个受损己方建筑。
- 一次性测试仅把这两个对象的 `+0x5B8` 从 `0` 改为 `1`；满血建筑和其他归属对象未被修改。
- 3 秒时生命值尚未变化，10 秒后 `CAWASH06` 从 `506/1000` 回升到 `674/1000`，`GAPILE` 从 `325/500` 回升到 `493/500`，且修理标志保持为 `1`。
- 结论：`+0x5B8=1` 会进入游戏原生修理流程，不需要直接写生命值；当前玩家归属筛选和一次性修理逻辑已验证成功。下一步可做持续开关、关闭时恢复原始修理标志，并测试建筑被摧毁/出售/转移时的安全性。

## 61. 自动修理持续服务启动

- 新增 `patcher/ra2_auto_repair_service.c`，生成 `build-vs\\Release\\ra2_auto_repair_service.exe`。
- 服务每 500ms 重新识别当前玩家和活动建筑，只对“己方、受损、修理标志为 0”的对象写入 `+0x5B8=1`；不修改生命值、不处理敌方对象、不改变游戏代码。
- 服务启动前校验 Steam `game.exe` 路径和基线 SHA-256；用户可用 `Ctrl+C` 停止服务。
- 当前服务已启动，后续应测试新建筑受损后是否自动进入原生修理流程，以及建筑出售/摧毁后服务是否安全跳过失效对象。

## 62. 自动修理响应优化

- 用户反馈旧版服务存在明显延迟；分析确认原因是每 500ms 都对整个进程地址空间做完整扫描。
- 已停止旧服务并改为两级检查：每 2 秒刷新建筑对象列表，每 100ms 只复核已缓存对象的类型、归属、生命值和修理标志。
- 快速检查仍会在写入前验证对象有效性，因此建筑出售、摧毁或进程重启后不会盲写旧地址。
- 优化版已重新编译并启动，当前等待用户验证新受损建筑的响应速度。

## 63. 自动修理延迟根因确认

- 用户复测后确认优化扫描器没有改变体感延迟。
- 服务日志已证明它能够及时命中 `GAPILE` 并设置 `+0x5B8=1`；随后建筑生命值按游戏原生节拍逐步恢复。
- 因此当前延迟来自原版修理计时/速率计算，而不是对象扫描。资料中的 `+0x5B8` 方案本身只负责进入修理状态，并不承诺瞬间修复。
- 当前功能目标需明确为“自动进入原生修理”还是“加速/即时修理”。若保持原生修理，应接受该延迟；若追求即时效果，下一步应单独定位原生修理速率计算，不直接锁定生命值。

## 64. 自动修理触发间隔修正

- 用户明确反馈的是“触发修理有延迟”。复核发现优化版服务的注释与实际循环不一致：快速检查实际每约 500ms 执行一次。
- 已将快速检查间隔修正为 50ms；对象列表仍每 2 秒刷新，避免全进程扫描造成高负载。
- 该改动只改善触发检测，不改变游戏原生修理速度；服务正在重新编译并启动验证。

## 65. 自动修理服务还原

- 用户确认触发延迟未因 50ms 检查间隔而改善，说明主要延迟不在当前服务循环间隔。
- 已停止持续服务，并执行一次关闭清理；当前己方建筑修理标志均为 `0`，没有后台服务或代码补丁残留。
- 后续不再继续压缩轮询间隔；下一步应研究原生修理触发事件或修理调用路径，避免通过高频扫描模拟事件。

## 66. 已完成任务复盘与纠错

本项目当前必须区分“单机功能验证通过”和“消费级产品完成”，两者不能等同。

### 已完成单机验证

- 金钱不减：已验证扣款路径跳过、自然收入正常、原始字节可恢复。
- 电力无负载：已验证负载累加跳过，关闭后可恢复正常负载计算。
- 即时建造与招募：建筑、步兵及多类生产队列已验证；完整取消队列、AI 和关闭恢复回归仍需补齐。
- 即时超武：原版超武及两个伞兵技能已完成单机释放验证；仍需统一封装不同对象编号。
- 解锁迷雾：开启、关闭和小地图恢复流程已验证；载入存档和任务切换仍需回归。
- 无视建筑距离：双调用点方案已验证远距离放置，并保留地形、水陆、占用和 AI 检查；关闭恢复已验证。
- 自动修理：基础 `RepairSell=0` 与驻军民用建筑维修标志方案均已验证，两个功能可独立开关。

### 必须纠正的实现表述

- 驻军民用建筑自动修理不是原版底层维修机构的完整扩展，而是外部服务筛选对象后设置 `+0x5B8` 维修标志；后续成品记录必须如实保留这一边界。
- 旧 DLL 注入、远程线程、硬件断点和进程内 Hook 仅属于历史实验，不属于当前有效实现路线。
- `ra2_instant_once.exe`、各类 probe、gate test 和一次性 patcher 是验证工具，不应直接称为正式修改器。
- “功能测试成功”不代表“消费级产品完成”；统一 UI、版本校验、开关状态、异常恢复、日志和完整回归测试仍未完成。

### 当前实际状态

- 游戏进程当前没有自动修理后台服务运行，基础自动修理也已关闭。
- 无视建筑距离、即时超武、迷雾和即时生产均应视为已验证但默认关闭的功能原型。
- 下一阶段应停止新增功能，统一整理已有功能的开关、校验、恢复和状态反馈。

## 67. 即时建造后的多单位共享出口：资料研究记录

本功能当前只进入资料收集阶段，不应用任何游戏补丁。研究范围限定为：建筑未出售、未摧毁的正常生产流程中，前一个单位仍在出厂动画时，后续即时完成单位不应被清理，而应允许多个单位共享同一出口。

### 已确认的结构线索

- 生产 CD 与出厂阶段是两个不同状态；当前项目已经验证队列 `+0x24` 只负责生产完成判定。
- Westwood 同源公开源码将生产建筑的职责拆成 `Factory_AI`、`Exit_Object`、`Exit_Coord`、`Find_Exit_Cell` 和 `Fire_Out` 等不同路径，说明“单位生产完成”和“单位离开建筑”不是同一事件。[EA BUILDING.CPP](https://github.com/electronicarts/CnC_Remastered_Collection/blob/master/REDALERT/BUILDING.CPP)
- 同源源码中存在 `RADIO_UNLOADED` 消息，用于表示对象已经完全离开建筑；这类“离开后清理”路径值得作为卡兵研究重点。[EA BUILDING.CPP](https://github.com/electronicarts/CnC_Remastered_Collection/blob/master/REDALERT/BUILDING.CPP)
- Ares/YRpp 资料显示 `FactoryClass` 同时持有当前 `Object` 和 `Production.Timer`，进一步支持“生产计时”和“当前出厂对象/出口状态”相互独立的判断。[YRpp StaticInits.cpp](https://github.com/Ares-Developers/YRpp/blob/master/StaticInits.cpp)
- OpenRA 的生产逻辑也把“生产完成”和“出口被堵塞”分开处理；这只能作为同源机制的旁证，不能直接当作 RA2 地址或实现结论。[OpenRA ProductionQueue.cs](https://github.com/worleydl/OpenRA-dev/blob/bleed/OpenRA.Mods.Common/Traits/Player/ProductionQueue.cs)

### 当前工作假设

卡兵更可能发生在单位已经创建、可以被批量选择，但工厂仍以单个当前对象或出口占用状态阻止后续对象出厂；目标是解除“单单位出口占用”或跳过“出口失败后清理”，而不是让后续单位排队等待，也不修改既有即时建造逻辑。

### 下一步只读研究顺序

1. 对兵营、重工分别记录“生产完成、动画中、首次可移动”三个阶段的对象变化。
2. 区分单位 Activity、建筑当前对象指针、出口坐标/占用和动画状态。
3. 检查后续队列被卡时，究竟是生产队列未清、出口检查失败，还是单位未发送离开完成消息。
4. 先建立字段差分和状态分类，再决定是放宽出口占用检查，还是保留后续单位并让其复用出口坐标。

### 目标行为

```text
单位 A：出厂动画未结束
单位 B：即时完成
→ B 不被清理
→ B 复用同一出口生成
→ A、B 可暂时重叠
→ 两个单位最终都恢复普通控制
```

这里的“共享出口”是逻辑目标；是否能同时叠加两套建筑门动画，仍需由当前 RA2 可执行文件的实际状态结构决定。

## 68. 共享出口方向的资料确认

- 已找到与本目标高度接近的 RA2 案例：一个出厂阶段异常、不可移动的单位会占住重工地面出口，之后生产的单位仍会生成，但会留在建筑内部。[Chronosphere 出厂异常记录](https://cnc-central.fandom.com/wiki/Chronosphere_%28Red_Alert_2%29)
- 该案例说明“出口被占用”本身可以阻止后续单位离开，且影响范围可能不只限于地面单位；这与当前即时生产时后续单位被清理/无法出厂的现象具有相同的出口阻塞特征。
- `ProductionAnim` 资料说明生产动画和单位自动离开建筑存在先后关系；动画结束前，自动出厂可能被阻塞。[ProductionAnim](https://modenc.renegadeprojects.com/ProductionAnim)
- 同源源码把 `Exit_Object`、`Find_Exit_Cell`、`Fire_Out` 和 `RADIO_UNLOADED` 分开处理，当前最值得追的是“出口失败后的处理分支”，而不是生产计时器。[EA BUILDING.CPP](https://github.com/electronicarts/CnC_Remastered_Collection/blob/master/REDALERT/BUILDING.CPP)

### 当前研究结论

新功能应尝试让多个生产完成对象共享一个出口状态：保留已完成单位，不执行出口失败清理，并在同一出口坐标继续触发正常出厂。既有即时建造逻辑保持不变。

## 69. 出口失败分支的同源源码确认

EA 公布的同源 Red Alert 源码给出了比“出口动画冲突”更具体的控制流：

- `Exit_Object` 对步兵/车辆先调用 `Find_Exit_Cell`，只有找到 `Can_Enter_Cell(cell) == MOVE_OK` 的邻近格，才继续 `Unlimbo`、设置移动任务并建立卸载联系。
- 找不到可进入格，或 `Unlimbo` 失败时，`Exit_Object` 返回 `0`。
- `Factory_AI` 收到返回值 `0` 后执行 `Factory->Abandon()`，随后删除当前 `Factory` 对象；返回值 `1` 才是暂时阻塞、稍后重试，返回值 `2` 才是正常出厂。
- 因而本功能最小可行的拦截面应是“出口暂时被占用时，把失败结果从 0 转成可保留/重试的路径”，或在出口检查层允许复用同一出口；直接改生产 CD、锁定资金、清空当前对象都不是正确切入点。[EA BUILDING.CPP](https://raw.githubusercontent.com/electronicarts/CnC_Remastered_Collection/master/REDALERT/BUILDING.CPP)

这同时解释了为什么简单地把单位瞬间完成会出现“后续单位消失”：只要当前 Steam 版对应路径仍把出口占用归类为不可恢复失败，后续完成对象就会触发放弃清理。下一步只读工作将围绕 `Exit_Object` 的返回值来源和 `Can_Enter_Cell/Unlimbo` 的失败分支定位当前可执行文件中的等价代码。

### 69.1 社区补丁的旁证与边界

Ares 文档把“快速连续出厂导致主工厂来不及处理出口周期”单独称为 Factory Load Sharing，并在原有逻辑上寻找其他同类工厂的出口。这证明“生产节奏”和“出口周期”确实是两个独立瓶颈，但它采用的是备用工厂分流，并不是本项目要求的同一出口叠加，因此只能作为定位线索，不能直接套用。[Ares Factory Load Sharing](https://ares-developers.github.io/Ares-docs/bugfixes/type2/factoryloadsharing.html)

当前 `game.exe` 静态检查已确认即时生产所用的 `0x004B9362` 附近主要是队列进度/完成处理，不能作为共享出口补丁点。下一步应转向出厂调用链；候选行为必须同时满足：保留完成对象、避免 `Abandon` 清理、允许再次使用原出口、且不改变地形/水陆/建筑占用判断。

### 69.2 Ares 源码定位到出厂搜索钩子（已判定不可直接迁移）

进一步读取 Ares/YRpp 源码后，曾推测其“备用出口”扩展直接挂在 `BuildingClass_KickOutUnit_FindAlternateKickout` 的 `0x004444E2`。但对当前 Steam 原版 `game.exe` 做真实反汇编后，`0x004444E2` 位于一个遍历房屋对象、检查类型标志并更新建筑状态的函数中，寄存器和控制流不符合 Ares 钩子所需的 `Src/Tst/Production` 参数布局。因此 Ares/YR 的地址不能直接迁移到本目标版本；该候选已撤销，仅保留源码层面的逻辑参考。[Ares Hooks.cpp](https://raw.githubusercontent.com/Ares-Developers/Ares/refs/heads/master/src/Ext/TechnoType/Hooks.cpp)

YRpp 的类布局还确认：`BuildingClass` 持有 `Factory` 指针，而 `FactoryClass` 分开持有 `Production`、队列和当前 `Object`；因此不能简单清零 `BuildingClass::Factory`，否则会破坏生产归属和后续队列。[YRpp BuildingClass.h](https://raw.githubusercontent.com/Ares-Developers/YRpp/master/BuildingClass.h)、[YRpp FactoryClass.h](https://raw.githubusercontent.com/Ares-Developers/YRpp/master/FactoryClass.h)

当前最有价值的候选点已从“生产完成函数”收敛到 `0x004444E2` 附近的 `KickOutUnit` 出厂搜索链。下一步需要用一次受控的双单位测试，区分该链上的两种情况：

1. 第二个单位尚未创建，只是队列等待；
2. 第二个单位已经创建，但因出口检查失败而被清理。

只有确认属于第 2 种，才进入分支级实验；实验目标是保留对象并复用原出口，不放宽地形、占用、水陆或敌我规则。

### 69.3 当前定位状态

- Ares 源码中的 `0x004444E2` 只确认了 YR/Ares 版本的备用工厂钩子；当前 Steam 原版反汇编已证明该地址不是同一函数，不能作为本版本补丁点。
- 当前版本必须重新从真实出厂对象创建、`Unlimbo`/出口格检查和清理调用的交叉引用定位，不能继续沿用 YR 地址假设。
- 要实现本项目目标，实验必须保留原有出口规则，只改变出口被动画中的前一个单位占用时的处理结果：不能转备用工厂时，不应走销毁/放弃路径，而应保留当前对象并再次执行原出口流程。
- 目前尚未写入 `0x004444E2`，也未改变任何既有修改器功能；下一步等待一次双单位现场状态样本后，才决定是否对该分支做最小化测试补丁。
- 已编译只读采集器 `build-vs/Release/ra2_factory_exit_snapshot.exe`，限制在当前对象池私有内存区；后台连续采样已停止，因为输出缓冲会造成实时判断误报。当前工具保留为“操作前后各执行一次”的只读快照工具，仅使用进程查询/读取权限，不写入游戏内存。已在当前 PID 27468 上完成一次实际读取，识别到 84 个活动对象，说明快照链路可用；这次仅是基线快照，尚未包含双单位出厂事件。

目前仍缺少当前 Steam `game.exe` 中的确切分支地址，因此下一步继续做静态调用关系和只读状态差分，不进入补丁测试。

### 69.4 新的最小实验假设

对照公开的同源实现后，候选逻辑进一步收敛为“区分暂时出口阻塞与真正无合法出口”，而不是粗暴放行出厂：`Exit_Object` 的返回值中，`1` 表示暂时阻塞、稍后重试，`0` 表示彻底失败并允许 `Factory_AI` 调用 `Abandon` 清理对象。若 Steam 版在“前一单位仍处于出厂动画”时错误地产生 `0`，最小修复应只把这一种阻塞情形映射到 `1`，并继续让 `Find_Exit_Cell`、`Can_Enter_Cell`、水陆类型和敌我占用检查决定其它失败结果。

因此当前不应修改 `0x004444E2`，也不应把所有失败统一改成重试；下一步应先完成当前 `game.exe` 的真实出厂调用链定位，再用现场样本确认第二单位是否已经生成且随后消失。

### 69.5 同源源码给出的更窄候选

同源 `BUILDING.CPP` 的武器工厂分支明确处理了“当前建筑仍处于 `MISSION_UNLOAD`”的情况：先尝试把对象交给另一座可用的同类工厂；没有备用工厂时直接返回 `1`，注释说明这是“仍在卸载前一个对象”，应稍后重试，而不是返回 `0`。这与本项目要求的“同一出口连续出厂”高度吻合，但只能作为逻辑参考，不能直接推出 Steam 原版的地址或字节。[Exit_Object 武器工厂分支](https://raw.githubusercontent.com/electronicarts/CnC_Remastered_Collection/master/REDALERT/BUILDING.CPP#L1905-L1920)

### 69.6 Steam 原版真实候选范围

对 `game.exe` 的当前版本进行反汇编后，生产/卸载控制流已从错误的 `0x004444E2` 移到更合理的 `0x00414D90` 附近：该函数检查建筑对象的生产关联指针 `this+0x494`、调用完成/合法性检查，并在后续路径调用 `0x004185B0`。后者接收“建筑 + 待出厂对象”并遍历地图候选格，随后由调用方继续执行对象出厂动作，行为上符合 `Find_Exit_Cell`/出口检查的职责。

当前最有价值的原版候选链为：

- `0x00414D90–0x00415067`：建筑生产/卸载状态机候选；
- `0x00414F44`、`0x00414F7B`：调用出口候选函数的两个分支；
- `0x004185B0`：接收待出厂对象并执行候选出口格搜索的函数候选。

这些位置只是静态候选，尚未写入补丁。下一步应先通过一次真实双单位事件验证 `0x004185B0` 的输入对象是否就是第二个已完成单位，再决定是否在 `0x00414D90` 的失败返回处恢复“暂时重试”。

已编译只读硬件断点跟踪器 `build-vs/Release/ra2_exit_hwtrace.exe`。它只设置线程级执行断点，不改写 `game.exe` 指令或数据；最近一次已附加到 PID 7632，等待候选入口被调用，并会记录寄存器、线程和栈参数。

随后通过独立只读寄存器采集器确认：当前游戏线程的 DR0–DR3 已实际装载为 `0x00414D90`、`0x00414F44`、`0x00414F7B`、`0x004185B0`，DR7 为执行断点启用状态。因此“断点完全没有设置成功”这一解释已排除；此前无命中更可能是采样期间没有触发这条出厂状态机，或候选函数只在特定完成/卸载阶段调用。下一步仍需一次明确的第二单位出厂事件来区分两者。

### 69.7 首次动态负证据

在当前 PID 7632 的实际对局中，车辆生产指针存在且 CD 从 42 降到 2，说明生产计时确实推进；随后使用快照和多入口硬件跟踪器观察，`0x00414D90`、`0x00414F44`、`0x00414F7B`、`0x004185B0` 均未命中。这排除了“仅仅因为没有进入对局/没有生产”的解释，但尚不能区分“候选地址错误”和“断点未成功设置”两种原因。下一步先验证跟踪器本身，再继续扩展原版真实出厂调用链。

因此候选不再是泛化修改 `Can_Enter_Cell` 或强制放行，而是确认 Steam RA2 对应分支是否因即时生产时序错误地落到了最终 `return 0`。若现场样本确认第二单位已出现后消失，最小实验应围绕该 `MISSION_UNLOAD`/出口动画状态分支，将其恢复为临时重试语义；其它无合法出口情况仍保持原版失败路径。

### 69.8 即时生产路径的动态确认

为捕捉“生产被卡掉”的瞬时过程，临时将一个线程硬件观察点放到已知的生产完成入口 `0x004B9362`。在即时生产服务运行期间，该入口被同一游戏线程以极高频率反复命中，寄存器样本稳定为 `EAX=1`、`ECX=0x11`，说明当前问题确实发生在“完成状态持续被推进/重复处理”的时序层，而不是生产按钮没有生效。

随后已安全解除调试跟踪，游戏 PID 7632 与即时生产服务仍在运行。该观察点没有写入游戏代码或数据，也未作为正式修复方案。当前研究重点进一步收窄为：即时完成后，生产状态机如何把完成对象交给出厂流程，以及出口被前一对象占用时为何没有进入可重试状态。下一步应重新附加低频、带节流的出口链跟踪，避免再次让高频完成入口淹没调试事件。

以上资料只用于建立逆向假设；由于公开源码不是当前 Steam RA2 可执行文件，不能直接迁移地址或调用约定。

### 69.9 连续即时生产现场的下一步判别

当前用户反馈为“持续快速生成时，大部分生产被卡掉”。结合已有观察点，首先要把“卡掉”拆成三个可观测结果：

1. 后续队列只完成计时，没有创建第二个单位对象；
2. 第二个单位对象已经创建，但在出口失败后被清理；
3. 第二个单位对象仍然存在，只是工厂的当前对象、出口占用或卸载状态没有释放。

即时服务对已知完成入口的重复命中，说明完成入口可能被连续触发，但这本身不能证明对象被清理。因此下一轮只读采集必须在同一工厂上做“开始前、首个单位出厂动画中、第二个单位完成后、数秒后”四个快照，并按对象地址、类型、所属方和活动状态做集合差分。

判定规则固定为：

- 对象从快照中消失：优先追出口失败返回值到清理调用；
- 对象持续存在但没有移动：优先追出口坐标、活动状态和卸载消息；
- 对象根本没有出现：优先追生产完成后对象创建/转交工厂的分支。

这一步不修改即时生产入口、不放宽地形或占用检查，也不改变既有产品模块；只有确认属于“前一个单位卸载期间被错误判定为不可恢复失败”后，才制作独立的最小分支实验。

已将只读采集器 `build-vs/Release/ra2_factory_exit_snapshot.exe` 增强为记录生产建筑对象的 `+0x494` 工厂关联指针及其相邻卸载候选字段，并在字段变化时输出差分。重新编译通过；本轮因游戏未运行，仅确认工具返回 `GAME_NOT_FOUND`，没有执行任何游戏内存写入。

### 69.10 出口链静态定位收敛

对目标 SHA-256 对应的 `game.exe` 做了只读反汇编：`0x00414F44` 和 `0x00414F7B` 确实是两个出口搜索调用点，二者分别在 `0x00414F49` 和 `0x00414F80` 立即把搜索返回值交给建筑的虚函数表 `+0x410`。出口搜索函数为 `0x004185B0`，其失败路径返回待出厂对象指针，成功路径返回出口候选结果；这使得“搜索返回值 → 工厂处理”成为当前最窄的动态验证面。

只读硬件跟踪器已改为记录这两个调用后的返回值，并修正硬件执行断点异常后的 Resume Flag 处理；已重新编译 `build-vs/Release/ra2_exit_hwtrace.exe`。仍未写入游戏代码或数据。下一次现场样本只需观察 `0x00414F49/0x00414F80` 的 `EAX`，即可判断卡兵是否进入出口失败处理，而无需先改动任何分支。

## 70. 转入消费级产品阶段

用户决定暂缓“即时生产下多个单位共用同一出口”功能。该方向保留研究记录，但不进入产品启动流程，也不再作为当前交付目标。

当前阶段改为统一封装已有单机功能，重点不再是增加功能数量，而是补齐版本保护、独立开关、事务式应用/恢复、进程生命周期、异常退出清理、状态显示和日志。详细实施门槛已整理至 `CONSUMER_PRODUCT_PLAN.md`。

已停止当前即时生产后台服务；游戏进程仍在运行，未留下磁盘补丁。下一步从统一安全底座开始，先整合金钱不减、电力无负载和即时建造与招募三个核心功能。

### 70.1 核心模块只读基线

已对当前运行中的 Steam `game.exe` 完成只读自检：SHA-256 与目标版本一致；金钱补丁字节为原始 `2B C7`，状态为 `UNAPPLIED`；生产字段管理器处于 `READ_ONLY`；电力补丁字节为原始值，状态为 `UNAPPLIED`。三次检查均未执行内存写入。

这证明当前可安全进入统一封装阶段。下一步优先建立共享的版本/进程/事务状态层，再把现有独立管理器迁移到同一套状态码和恢复保护上。

### 70.2 模块状态层首轮实现

已新增 `product/ra2_product_module.h/.c`，统一管理八个功能模块的状态、原值快照和应用/恢复事务。首轮单元测试已通过：重复开启返回 `ALREADY_ON/BUSY`；恢复时当前值与快照不一致会进入 `ERROR`，拒绝盲目恢复；只有快照一致时才回到 `OFF`。该层目前不执行游戏内存读写，下一步再将现有管理器逐个迁移到它。
