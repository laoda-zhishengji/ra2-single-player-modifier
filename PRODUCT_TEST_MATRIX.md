# 消费级修改器验收矩阵

状态定义：

- `PASS`：已有实机证据，且范围与测试项一致。
- `PARTIAL`：功能已验证，但缺少产品生命周期或边界验证。

## 统一入口无游戏冒烟测试

已完成以下只读/无游戏环境测试：

- 模块状态层单元测试：`MODULE_STATE_TEST=PASS`；
- 统一入口 `--status`：正确报告模块注册表就绪、两个修理模块 `OFF` 和 `game=NOT_FOUND`；
- 统一入口 `--repair-status`：正确报告两个修理服务均为 `OFF`；
- 统一入口 `--repair-all-off`：无服务时安全返回，未残留修理进程；
- 未知命令：返回使用说明和错误码，不执行任何子模块。
- 界面程序：Win32 编译通过；游戏未运行时的按钮行为尚未执行实机回归。
- 统一入口完整 `--status`：依次汇总核心、修理、生产、超武、迷雾和建筑距离状态；无游戏时均返回不可用/关闭状态，不执行写入。
- 统一入口 `--self-check`：全部必需组件存在，输出 `SELF_CHECK=PASS`。
- 便携发布包：生成 11 个产品组件和说明文件；发布目录自检通过，研究工具未进入发布目录。
- 发布脚本完整性：复制完成后自动执行发布目录自检，本轮输出 `PACKAGE_SELF_CHECK=PASS`。
- 发布目录隔离：异常文件白名单检查通过，当前没有研究工具混入。

本轮未启动游戏，因此没有把这些结果误记为实机功能回归。
- `TODO`：尚无足够证据，不能进入发布版。
- `BLOCKER`：失败时必须阻止发布。

## 通用安全与生命周期

| 编号 | 测试 | 预期 | 当前 |
|---|---|---|---|
| G-01 | 匹配 Steam `game.exe` | 版本通过，允许读取状态 | PASS |
| G-02 | 修改游戏文件哈希后启动 | 拒绝所有写入 | TODO / BLOCKER |
| G-03 | 非目标路径存在同名进程 | 不附加、不写入 | PASS |
| G-04 | 重复点击开启 | 不重复打补丁或分配线程 | TODO / BLOCKER |
| G-05 | 关闭已开启功能 | 只恢复本模块保存的原值 | TODO / BLOCKER |
| G-06 | 关闭未开启功能 | 不写入、不报错 | TODO |
| G-07 | 游戏正常退出 | 后台线程结束，句柄释放 | TODO / BLOCKER |
| G-08 | 游戏异常退出 | 不修改磁盘文件，后台线程结束 | TODO / BLOCKER |
| G-09 | 多功能同时开启/关闭 | 模块互不覆盖，按逆序恢复 | TODO / BLOCKER |
| G-10 | 日志导出 | 含版本、PID、状态、失败原因，不含账号信息 | TODO |

### 已完成的控制层闭环

- 金钱模块：开启、状态读取、关闭恢复通过。
- 电力模块：开启、状态读取、关闭恢复通过。
- 两模块分别操作时互不改变对方状态。
- 即时建造字段：快照、一次性应用、校验恢复通过；任务与队列回归仍待完成。

## 功能回归

| 功能 | 核心行为 | 关闭恢复 | 任务切换/存档 | AI 边界 | 当前 |
|---|---|---|---|---|---|
| 金钱不减 | 购买不扣，收入正常增加 | 待统一验证 | TODO | TODO | PARTIAL |
| 电力无负载 | 耗电建筑不降低可用电力 | 已有独立验证 | TODO | TODO | PARTIAL |
| 即时建造与招募 | 建筑、步兵、车辆、飞机等立即完成 | 部分验证 | TODO | TODO | PARTIAL |
| 即时超武 | 发射后再次可用，界面状态一致 | TODO | TODO | TODO | PARTIAL |
| 解锁迷雾 | 地图和小地图可见 | 已有验证 | TODO | 不影响敌方逻辑 | PARTIAL |
| 无视建筑距离 | 只跳过距离检查 | 已有验证 | TODO | 已有边界验证 | PARTIAL |
| 原生自动修理 | 普通己方建筑按原生周期修理 | 已有验证 | TODO | 不影响敌方 | PARTIAL |
| 驻军民用建筑修理 | 只修玩家所属受损驻军民用建筑 | 部分验证 | TODO | TODO | PARTIAL |

## 必测组合

### 组合 A：核心三项

开启金钱不减、电力无负载、即时建造与招募；连续建造、购买、拆除和取消生产；逐项关闭并确认其他两项不受影响。

### 组合 B：地图与建筑

开启解锁迷雾和无视建筑距离；测试雷达存在、雷达出售、重新建造雷达、任务载入和小地图恢复；确认水陆、占用、资源和 AI 规则仍有效。

### 组合 C：修理

分别测试普通己方建筑、已驻军民用建筑、敌方建筑、驻军退出、建筑出售和建筑摧毁；两个自动修理开关必须可以独立关闭。

### 组合 D：生命周期

在任务中开启功能，保存、载入、切换任务、退出游戏、重新进入游戏；确认没有旧指针复用、后台服务残留或状态误报。

## 发布门槛

以下项目全部满足前，产品只能标记为“测试版”：

1. G-02、G-04、G-05、G-07、G-08、G-09 全部通过；
2. 每个功能至少完成一次“开启—验证—关闭—验证”；
3. 至少完成组合 A、B、C 的完整回归；
4. UI 显示状态与只读状态检查器结果一致；
5. 任何未知字节、未知版本或未知对象状态都拒绝写入；
6. 研究探针和一次性实验工具不出现在发布目录和普通启动流程中。

## 当前下一项

先实现 G-04/G-05 所需的统一模块状态记录和恢复保护，再开始制作普通用户界面。没有这层保护，不接入更多功能，也不制作“全部开启”按钮。
