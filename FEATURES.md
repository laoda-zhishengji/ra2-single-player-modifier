# 修改器功能规格

适用范围：Steam 版《红色警戒2》原版，单机战役与遭遇战。

## 通用要求

- 所有功能默认关闭，由用户单独启用。
- 修改器只操作当前匹配的 `game.exe` 进程，不修改磁盘上的游戏文件。
- 每个功能都必须有状态显示、失败提示和关闭恢复。
- 版本校验失败时禁止启用任何功能。
- 不支持联机、局域网或任何网络对战模式。
- 游戏异常退出后不应留下磁盘补丁。

## 功能清单

### 1. 金钱不减

行为：建造、购买和部署单位时不扣除玩家资金；收入、金币箱和奖励仍可正常增加资金。

验收：连续购买多个不同价格的单位，界面资金稳定；收入事件仍能增加资金；关闭后恢复正常扣款。

预期方式：外部进程内存代码补丁，阻止玩家支出路径，而不是定时锁定显示数值。

当前状态：单机测试已通过；仍属于功能原型，尚未接入统一消费级界面。

优先级：P0。

### 2. 电力无负载

行为：电厂提供的电力不因建筑和设施消耗而减少，电力状态保持在可用范围。

验收：建造大量耗电建筑后仍保持正常供电；拆除电厂、关闭功能或退出后恢复正常电力计算。

预期方式：定位电力供需计算或电力扣减路径，优先对玩家对象做条件补丁。

当前状态：单机测试与关闭恢复已通过；仍属于独立功能工具，尚未完成统一封装。

优先级：P0。

### 3. 即时建造与招募

行为：建筑、车辆、步兵和飞机完成生产所需时间立即归零或直接完成。

验收：不同生产建筑、不同单位类型均可立即完成；队列逻辑不损坏；取消生产仍能正常工作。

预期方式：修改生产计时完成条件或生产耗时结果。

当前状态：建筑、步兵及多类生产队列已完成实机验证；取消队列、AI 和完整回归仍需纳入正式版本测试。

优先级：P0。

### 4. 即时超武

行为：超级武器充能完成后立即可用，或跳过重新充能等待。

验收：至少测试所有原版超级武器；发射后可再次使用；不影响普通武器和建筑状态。

预期方式：修改超级武器计时器或冷却判断，不改发射流程。

当前状态：原版超武与两个伞兵技能已完成单机释放验证；不同超武对象的完整回归和统一开关仍未封装。

优先级：P1。

### 5. 解锁迷雾

行为：地图区域保持可见，隐藏区域和已探索区域不再恢复为迷雾。

验收：进入任务后地图完整可见；移动单位、载入存档和触发剧情后仍保持可见；关闭后按原版规则恢复。

预期方式：修改玩家视野/迷雾判定或地图可见状态。

当前状态：开启、关闭和小地图恢复流程已完成实机验证；仍需整理为统一开关并覆盖载入存档/任务切换。

优先级：P1。

### 6. 无视建筑距离

行为：建造时忽略与己方建筑、基地或施工范围相关的距离限制。

验收：在原版不允许的位置尝试建造；合法地形、占用检测和资源条件仍然有效；关闭后恢复距离限制。

预期方式：修改建筑位置合法性检查中的距离条件，只跳过距离分支。

当前状态：双调用点方案已通过远距离放置、地形、水陆、占用和 AI 边界测试，关闭恢复已通过；尚未接入统一界面。

优先级：P2。

### 7. 自动修理建筑

拆分为两个独立开关：

#### 7.1 原生自动修理

行为：通过运行时 `RepairSell=0` 启用游戏原生的 AI 修理决策，让普通己方建筑按原版修理周期和资金规则工作。

验收：普通己方建筑可自动修理；不直接锁定生命值；关闭后恢复原版行为。

预期方式：只修改当前任务加载后的 IQ 规则值，不修改磁盘上的 `rules.ini`。

当前状态：单机功能已验证，当前关闭；尚未接入统一产品开关。

优先级：P2。

#### 7. 驻军民用建筑自动修理

行为：单独处理己方、受损、已驻军的民用建筑；不改变 `RepairSell` 全局规则，不影响敌方或驻军战斗逻辑。

验收：驻军前后均可区分；仅玩家所属对象受影响；驻军退出、建筑摧毁和关闭功能后状态正常。

当前状态：已验证可用，但采用外部对象扫描并设置原版 `+0x5B8` 维修标志；不直接写生命值。该实现不是原生底层挂接，需要作为独立服务开关管理。

预期方式：只筛选玩家所属、受损且属于民用类型的对象，设置原版维修标志；不改变普通建筑、敌方对象或驻军战斗逻辑。

优先级：P2。

## 推荐开发顺序

1. 金钱不减
2. 电力无负载
3. 即时建造与招募
4. 即时超武
5. 解锁迷雾
6. 无视建筑距离
7. 自动修理建筑

## 首版发布门槛

当前结论：核心功能多数已完成单机原型验证，但尚未达到首版发布门槛。还缺统一版本校验、功能开关、补丁状态显示、异常恢复、日志和完整回归测试。
