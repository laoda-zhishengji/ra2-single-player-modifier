# 阶段 B：最小注入验证

本阶段只验证 DLL 的加载与卸载日志，不读取、写入或 Hook 游戏逻辑。

目标必须是已识别的 Steam 原版 `game.exe`，且路径与基线一致。该验证器不包含隐藏、持久化、联机或反检测功能。

构建要求：Windows、CMake、可生成 Win32 目标的 C 编译器。由于目标游戏是 32 位，最终 DLL 和加载器必须编译为 Win32，而不是 x64。

验收信号：

- 加载器只接受匹配路径的 `game.exe`。
- DLL 加载后在 `%TEMP%\\ra2_modifier_probe.log` 写入 `attach`。
- 游戏退出时写入 `detach`（进程被强制终止时可能没有 detach）。
- 游戏主菜单和单机流程行为不被改变。

资金定位工具 `ra2_money_scan.exe` 只读扫描匹配的 32 位整数，不执行写入。正式功能前必须通过至少两次不同资金值的交叉筛选。

当前原型 `ra2_money.dll` 使用已验证的主模块偏移 `0x4373CC` 和 `0x4373D0`，每 100 毫秒写入 `999999`。它只是阶段 C 的验证原型，尚未达到最终消费级版本的版本校验、界面开关和安全卸载要求。

`ra2_money_no_decrease.dll` 是新的逻辑原型：接受资金增加，只在检测到资金低于上一次稳定值时恢复该稳定值。它同样仅用于当前 Steam 原版单机测试，尚未达到正式产品标准。

`ra2_money_hook.dll` 是代码级原型：校验 `game.exe+0x94670` 的原始指令后，在资金写入前比较新旧值，阻止下降并允许增加。它不使用定时器轮询，也不写入固定金额。

当前推荐的外部管理器为 `build-vs\\Release\\ra2_patch_manager.exe`：

- 不带参数：只做进程路径、SHA-256、补丁字节和状态检查，不写入游戏。
- `--apply`：仅在版本与原始字节均匹配时，把 `game.exe+0xE53A9` 的 `2B C7` 临时改为 `90 90`，跳过扣款减法。
- `--restore`：仅在当前字节为 `90 90` 时恢复为原始 `2B C7`。

该管理器不会修改磁盘上的 `game.exe`。正式启用前仍需在单机任务中验证玩家收入、购买、AI 和退出恢复行为。
