# 发布验收清单

## 已通过的离线检查

- 统一入口 `--self-check`：发布目录组件齐全。
- 发布脚本 `PACKAGE_SELF_CHECK=PASS`。
- 模块状态层单元测试：`MODULE_STATE_TEST=PASS`。
- 无游戏环境下状态查询不执行游戏内存写入。
- 无游戏环境下修理服务状态为 `OFF`。
- 未知命令会被统一入口拒绝。
- C 产品组件使用静态 MSVC 运行库；统一入口不依赖额外 VC++ 运行库 DLL。
- 消费级 UI 已通过启动回归：无边框标题栏、游戏图标、功能图标、分组导航、整行切换和操作结果反馈正常。
- UI 自动连接监视已通过静态与运行检查：游戏未连接或尚未进入任务时不会解锁功能；Hub 状态码不再把 `NOT_IN_TASK` 误报为整体失败。
- 发布包包含 UI 视觉素材，`PACKAGE_SELF_CHECK=PASS`。

## 下一次进入游戏后的实机顺序

1. 启动 UI，确认版本状态为匹配；主菜单阶段应显示“等待进入任务”，所有功能保持禁用。
2. 进入单人任务或遭遇战，确认 UI 自动切换为可操作状态，所有功能仍默认关闭。
3. 分别开启并验证金钱、电力，随后分别关闭并确认恢复。
4. 点击“即时生产：开关”，验证自动捕获、开启、关闭和安全还原。
5. 点击“即时超武：开关”，验证自动捕获、开启、发射、关闭；若对象状态已变化，必须拒绝恢复而不是强行覆盖。
6. 验证迷雾开启、关闭、小地图和雷达建筑变化。
7. 验证建筑距离开启、关闭，以及地形、水陆、占用和 AI 约束。
8. 分别验证普通自动修理和驻军民用建筑修理的独立开启/关闭。
9. 退出游戏，确认两个修理服务自动退出且 UI 状态回到不可用。

## 发布阻断项

- 任何版本不匹配仍能写入。
- 关闭功能无法安全恢复。
- 游戏退出后后台服务残留。
- 任务切换或存档载入复用旧指针。
- UI 状态与只读状态不一致。
